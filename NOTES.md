# 개발 노트 / 백로그

> 세션 간 인수인계용. "내가 추가하려던 기능 뭐였지?" 하면 이 파일을 보면 됨.
> 새 세션 시작 시 이 파일부터 확인.

---

## 인수인계 로그  (Claude·Codex 공용 상황판)

> **채팅은 사라진다 → 이 로그 + PR 설명만 믿는다.**
> 호칭 — **클**=Claude, **코**=Codex, **둘**=둘 다, **나**=사용자 결정.
> 역할 — **클**: 큰 기능·메인 개발 / **코**: 리뷰·버그·DB/보안/구조·별도 PR·쉬운 요약 / **나**: 머지·보류·다음 결정.
> 흐름 — 작업한 AI가 아래 5줄을 최신화 → 다른 AI에게 "NOTES 인수인계 로그부터 읽고 이어가".
> (전체 원칙: `AI_COLLABORATION.md`)

_갱신: 2026-07-25 · 클_

- **현재 상태:** 앱 배포·사용 중. PR #1~#5 반영(#2 원근보정만 revert). 메일(아웃룩식 서명+Gmail/기본/Outlook 선택), 크롭 화면 안정화, 일괄 중복 후보 선택 저장, 회사 묶음 편집·검색. **신규(클): 미팅 캘린더** — 명함 상세 `미팅 기록`에서 날짜·활동·메모 → **무료 Gemini가 Salesforce식 영문 활동기록 생성**(`sf-activity.md` 지침) → 편집·복사, `미팅` 탭에 날짜별 모아보기. ⚠️ **`0009_meetings.sql` Supabase 실행 필요(안 하면 미팅 저장 안 됨).**
- **진행 중:** 없음.
- **열린 PR:** 없음.
- **다음 우선순위:** 사용자가 0009 실행 후 미팅 기록·SF노트 생성·복사 동작 확인. 그 다음 기본 사진 인식(Gemini) 품질 개선.
- **결정 필요:** `0009_meetings.sql` 실행(사용자). 미팅 캘린더 다음 단계(월간 뷰/.ics)는 나중. 내 정보 localStorage vs Supabase 동기화.

---

## 현재 상태

앱은 폰에서 사용 중 (Vercel 배포 완료). 핵심 기능 전부 동작:
촬영/사진업로드 → 자동크롭 보조 → Gemini 추출(성/이름 분리) → 저장 →
목록(검색·필터·정렬·초성섹션·다중삭제) → 상세 편집/삭제/재인식/사진관리 →
태그 · 회사그룹 · 중복감지/병합 · 사람연결(이직이력) · 대표명함 지정 ·
여러 명함 일괄 인식(크롭·태그) · CSV/vCard 내보내기.

브랜치: `claude/business-card-collector-webapp-edaynx`

---

## 백로그 — 사용자가 원했지만 아직 안 한 것

### 우선순위: 검토 필요
- [x] **① 문서 스캔 정리(더러운 옛 명함)** — 크롭 화면(ImageCropper)에 필터 3종
  추가: 원본 / 선명(컬러 대비·채도↑) / 흑백 문서(grayscale+대비↑, 배경 하얗게).
  CSS 필터로 실시간 미리보기 + `ctx.filter` 로 결과에 그대로 구움(WYSIWYG).
  모든 크롭 경로(앞/뒤·일괄·상세) 공통 적용.
  - 아직: 원근 보정(기울기 자동)은 OpenCV 필요 → 무거움. 수동 크롭+회전으로 대체 중.
  - 아직: 지역 적응형 이진화(진짜 스캐너 흑백)까지는 안 함 — 전역 필터로 근사.
- [ ] **③ 조직 위계(본부/실/팀/파트 트리)** — 하나투어·모두투어 같은 큰 조직.
  - 가벼운 시작: "회사 → 부서 2단계 뷰"(회사 탭에서 부서별 접기).
  - 완전한 트리: department 문자열 파싱/구조화 필요 → 큰 작업. 데이터 쌓인 뒤 실제 모양 보고.

### 🗓️ 미팅 캘린더 + Salesforce 활동기록 (1~3단계 구현됨 · 0009 실행 대기)
[x] ① meetings 테이블(0009) + 명함 상세 `MeetingLog`(추가/목록/삭제)
[x] ② `/api/meetings/generate` — 무료 Gemini가 sf-activity.md 지침으로 SF 영문노트 생성 + 복사
[x] ③ `/calendar` 미팅 탭(날짜별 모아보기) + TabBar 추가
[ ] ④ (나중) 월간 달력 뷰, `.ics` 캘린더 추가, 녹음/음성 — 보류
아래는 원래 계획 원문 ↓
사용자 확정 스코프 — **SF API 연동 X, 녹음 X, 유료 STT X** (전부 무료·기존 도구로):
- **미팅 기록:** 명함(사람)에 미팅 붙여 저장. Supabase `meetings` 테이블(owner_id, card_id,
  meeting_date, activity(dinner/call/site visit…), raw_notes, sf_note, created_at) + RLS.
- **SF식 자동 정리:** 사용자가 거친 메모/키워드 입력 → **무료 Gemini**로 `lib/prompts/sf-activity.md`
  지침대로 영어 활동기록 생성. 항상 "MJ had a dinner meeting with ~" 형식. 이름은 내 정보(`/me`)에서.
  → 사용자 편집 가능 → 저장 → **복사 버튼**으로 Salesforce에 붙여넣기(연동 대신 복사).
- **캘린더 뷰:** 미팅을 날짜별 목록 / 간단 월간 뷰. 명함 상세엔 "미팅 기록" 섹션.
- **단계:** ① meetings 테이블+명함 상세 미팅 추가/목록 → ② Gemini SF노트 생성+복사 →
  ③ 캘린더 뷰 → (나중·선택) `.ics` 캘린더 추가, 녹음/음성.
- 파일: `lib/prompts/sf-activity.md`(작성됨), 새 `supabase/migrations/0009_meetings.sql`,
  `app/api/meetings/*`, `components/MeetingLog.tsx`, 캘린더 페이지 — 코 작업과 안 겹치게 새 파일 위주.

### 그 외 (대화 중 언급, 미구현)
- [ ] **태그 이모지 아이콘** — 노션 스타일. `tags.icon` 컬럼 추가 필요.
- [ ] **태그 계층/대분류→소분류** — 스코프 주의(심플 원칙).
- [ ] **ㄱㄴㄷ 빠른 색인 바** — 목록 오른쪽에 초성 스크러버(탭하면 점프). 명함 수백 장 넘으면 유용.
- [ ] **크롭 핀치 줌/팬** — 손가락으로 이미지 확대·이동하며 정밀 크롭. (지금은 명함별 확대로 우회)
- [ ] **미세 각도 보정** — 살짝 기운 것 몇 도 단위 보정(지금은 90° 회전만).
- [ ] **여러 명함에 태그 일괄 편집** — 목록 다중선택으로 태그 달기(지금은 다중삭제만).
- [ ] **구글시트 자동 동기화** — 원래 계획 15단계(선택). 서비스계정 방식. 지금은 수동 CSV로 대체 가능.
- [ ] **일괄 촬영/업로드(여러 장 연속)** — 한 장씩 말고 여러 장 큐로. (여러 명함 한 장 스캔은 이미 됨)

---

## 완료된 사용자 요청 (참고)
- 성/이름 한글·영문 분리 + 전체이름 자동 생성
- 회사/이름 정렬 가나다·역순, 회사 탭 정렬, 초성 섹션 헤더
- 저장/수정 후 목록 이동, 세로형 명함 썸네일 온전히 표시
- 사진 업로드(앨범) 복원, 크롭 회전
- 여러 명함 일괄 인식 + 명함별 확대 크롭 + 일괄 태그 + 명함별 뒷면 추가(빈 칸 채우기)
- 일괄: 뒷장 시트 한 번에 촬영 → 전화·이메일·회사로 앞면과 자동 매칭(못 짝진 건 수동 지정)
- 크롭 자동 맞춤(배경 대비 카드영역 감지) + 손글씨/취소선 수정 반영(프롬프트)
- (보류) 원근 보정 크롭(quad deskew) — 구현했으나 체감 이득 작아 revert. 이력 8352ce2 보존, 나중에 제대로 할 때 부활.
- 회사 묶음(관리 레이어) + 묶음 편집·회사 검색(코 PR #4). 원본 회사명은 보존.
- 내 정보(아웃룩식 메일 서명) + 명함 상세에서 "메일 보내기"(폰 메일 앱, 회사 메일 OK)
- 같은 회사면 태그 자동 제안
- 사람 연결(이직), 대표 명함 지정, 다중 선택 삭제

## 진행 원칙
- 매 단계 `npm run build` 통과 후 커밋·푸시 (Vercel 자동 배포).
- 시크릿/명함 데이터/이미지 커밋 금지.
- 새 패키지 추가 커밋은 pull 후 `npm install` 필요.
- Supabase 컬럼 추가 시 `supabase/migrations/` 에 SQL 두고 사용자가 대시보드에서 실행.
