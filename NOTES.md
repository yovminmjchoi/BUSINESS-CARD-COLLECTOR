# 개발 노트 / 다음 할 일

> 세션 간 인수인계용 메모. 새 세션 시작 시 이 파일부터 확인.

## 현재 진행 (2026-07-13)

완료: 1~10단계 (`.gitignore`~태그 관리). 상세 체크리스트는 `README.md` 하단 참고.
브랜치: `claude/business-card-collector-webapp-edaynx`

다음 예정: **11단계 — 회사별 그룹 뷰** (`company_normalized` 기준으로 같은 회사 명함 묶기, `/companies` 탭 채우기)

## 사용자 피드백 (10단계 태그 관련) — 다음에 반영 검토

1. **태그 색 종류가 너무 적다** → 색 팔레트 확장 (현재 `components/TagSelector.tsx`의 `TAG_COLORS` 7개).
2. **노션 스타일 아이콘(이모지)** 을 태그에 넣을 수 있으면 좋겠다.
   - `tags` 테이블에 `icon`(emoji) 컬럼 추가 마이그레이션 필요 → 태그 관리/선택/칩 표시에 반영.
3. **태그 화면에서 삭제 버튼이 바로 노출** → 실수 위험. 삭제를 한 단계 감추거나(예: 메뉴/스와이프) 확인 강화.
4. **태그 계층화** — 태그 목록화해서 보기, 또는 대분류 → 소분류 구조.
   - 스코프 주의: "심플하게"가 프로젝트 헌법. 계층은 무거워질 수 있으니 도입 전 필요성 재확인.
   - 가벼운 대안: 태그 그룹(색/이름 프리픽스)만으로 시각적 분류.

## 진행 원칙 리마인더
- 매 단계 끝 `npm run build` 통과 확인 후 커밋·푸시.
- 시크릿/명함 데이터/이미지는 절대 커밋 금지 (`.gitignore` 유지).
- 새 컴포넌트 pull 후 dev에서 `reading 'call'`류 뜨면 `.next` 지우고 재시작.
